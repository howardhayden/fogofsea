# Visual smoothness reconstruction red-team ledger

Date: 2026-09-12 · status: active, failures preserved

| Attack / failure | Result | Correction or boundary |
| --- | --- | --- |
| Previous uncommitted workspace was pruned | Failed; work was unrecoverable | Reconstructed on a named branch; retain this loss as evidence and commit isolated slices. |
| Checked-in dependency tree lacked AJV 2020 entry | Initial release check blocked | Reconciled locally from lockfile; rerun passed. Do not call the first run a pass. |
| Browser executable absent | Blocked before launch; zero assertions | Preserve exact path and classify as environment-blocked, never product-tested. |
| Infer TSUNAMI from large waves or storm visuals | Requirement retained; first implementation rejected | The unversioned timeline changed the version-1 matrix wire shape and invalidated existing played challenge saves. It was removed. Any future implementation requires explicit matrix/save versioning and cannot infer mechanics from pixels. |
| Capability manufactures random contacts | Baseline failed truth boundary | Contact planner now shape-checks caller-supplied estimate records and filters by sensing; it does not establish authority. The current caller supplies none, so live output is zero. Positive provenance/persistence/browser semantics remain unimplemented. |
| Getter/prototype smuggles contact data | Rejected | Own data-descriptor snapshot; no getter invocation. |
| Average FPS presented as smoothness | Rejected by requirements | Store interval percentiles, maxima, and hitch clusters. |
| 60 Hz-friendly modulo scheduler aliases at 90/120 Hz | Rejected | Monotonic deadline scheduler with skipped-deadline advancement. |
| Governor oscillates | Rejected | Adjacent tiers, 3-poor/8-good hysteresis, 20-second cooldown. |
| Reduced motion still runs RAF | Open | Requirement remains specified until event-driven renderer integration and execution evidence. |
| Whole-model Dream emission reveals/softens hard structure | Baseline, tiny-light, and shell-era corrections rejected | Production geometry and directly lit materials remain unchanged and receive no added emissive term. Renderer-owned emission-only proxies sample authorized native geometry into a bounded, view-conditioned three-scale exterior field with no halo mesh or scene light. Creature participation is decorative and confers no contact, sensing, or scoring authority. |
| Renderer mounted behind privacy modal | Open | Must gate the lazy tactical module on accepted storage choice; browser evidence pending. |
| Emulator described as thermal evidence | Rejected | Physical-device thermal evidence remains a doubt. |
| Blender added because available | Rejected | No qualified need; no dependency added. |

## Preserved open attacks

The final review must still exercise: denied nested partitions; revoked authorization and regrant; geometry bombs; hidden population as a quality oracle; uncertain marker motion toward true coordinates; lost-contact wake/glow retention; shared RNG/time with adjudication; WebGL loss; deleted semantics under quality pressure; combined flash/luminance; glass compositor pressure; impossible weather combinations; RAF behind overlays; stale worker responses; numeric-seed mismatch; relational-placement lock conflicts; oversized direct solver input; and browser attributes masked by launch failure.
